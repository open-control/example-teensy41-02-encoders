; Open Control - Example 02: Encoders
; Rotary encoders sending MIDI CC

[platformio]
default_envs = release

[env]
platform = teensy
board = teensy41
framework = arduino
build_flags = -std=gnu++17 -D USB_MIDI_SERIAL -I include

; ============================================================================
; Release: uses GitHub repos (standalone, no local deps required)
; ============================================================================
[env:release]
lib_deps =
    https://github.com/open-control/hal-teensy

; ============================================================================
; Development: uses local repos via symlink
; Usage: pio run -e dev
; ============================================================================
[env:dev]
lib_deps =
    open-control=symlink://../framework
    open-control-hal-common=symlink://../hal-common
    oc-hal-teensy=symlink://../hal-teensy
